<Window x:Class="Exam.App.MainWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Popryzhenok - agents" Width="1100" Height="740"><DockPanel Margin="12"><StackPanel DockPanel.Dock="Top" Orientation="Horizontal"><TextBox Width="240" Text="{Binding Search,UpdateSourceTrigger=PropertyChanged}"/><ComboBox Width="160" Margin="6 0" ItemsSource="{Binding Types}" SelectedItem="{Binding SelectedType}"/><ComboBox Width="160" ItemsSource="{Binding Sorts}" SelectedItem="{Binding SelectedSort}"/><Button Content="Add" Command="{Binding AddCommand}" Margin="6 0"/><Button Content="Edit" Command="{Binding EditCommand}"/><Button Content="Delete" Command="{Binding DeleteCommand}" Margin="6 0"/><TextBox Width="70" Text="{Binding MassValue}"/><Button Content="Set priority" Command="{Binding MassCommand}"/></StackPanel><StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Center"><Button Content="Prev" Command="{Binding PrevCommand}"/><TextBlock Text="{Binding Counter}" Margin="12 4"/><Button Content="Next" Command="{Binding NextCommand}"/></StackPanel><ListView ItemsSource="{Binding PageItems}" SelectedItem="{Binding Selected}"><ListView.ItemTemplate><DataTemplate><Border BorderBrush="#aaa" BorderThickness="1" Margin="0 4" Padding="10" Background="{Binding Highlight}"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="90"/><ColumnDefinition/><ColumnDefinition Width="210"/></Grid.ColumnDefinitions><Image Source="/resources/picture.png" Width="72" Height="72"/><StackPanel Grid.Column="1"><TextBlock FontWeight="Bold" FontSize="16"><Run Text="{Binding Type}"/><Run Text=" | "/><Run Text="{Binding Title}"/></TextBlock><TextBlock><Run Text="Phone: "/><Run Text="{Binding Phone}"/><Run Text=" Email: "/><Run Text="{Binding Email}"/></TextBlock><TextBlock><Run Text="Priority: "/><Run Text="{Binding Priority}"/><Run Text=" Sales 365: "/><Run Text="{Binding SalesCount365}"/></TextBlock></StackPanel><StackPanel Grid.Column="2" HorizontalAlignment="Right"><TextBlock Text="{Binding Discount,StringFormat={}{0}%}" FontSize="22" FontWeight="Bold"/><TextBlock Text="discount"/></StackPanel></Grid></Border></DataTemplate></ListView.ItemTemplate></ListView></DockPanel></Window>
